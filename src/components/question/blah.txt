

const questions = props.questions.map((index, question) =>
  <ol className="question">
    <li>{question[index]}</li>

    const answers = question[index].map((index, answer) =>
      <ol>
        <li>{answer.answerText}</li>
      </ol>
    )
  </ol>
)
